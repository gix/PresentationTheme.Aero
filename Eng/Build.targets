<Project>
  <Import Project="Common\Postamble.targets"/>

  <PropertyGroup Condition="'$(MSBuildProjectExtension)' == '.csproj' and '$(IsWpfTempProject)' != 'true'">
    <DocumentationFile>$(OutputPath)$(AssemblyName).xml</DocumentationFile>
  </PropertyGroup>
</Project>
