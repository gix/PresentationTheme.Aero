<UserControl
    x:Class="ThemeTestApp.Samples.WpfListViewSample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="500"
    d:DesignWidth="300">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="0"/>
    </Grid.ColumnDefinitions>
    <ListView Margin="0,0,0,0" Style="{DynamicResource Aero2ListView}" x:Name="lv1">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Item1}"/>
          <GridViewColumn Header="Größe" DisplayMemberBinding="{Binding Item2}"/>
          <GridViewColumn Header="Typ" DisplayMemberBinding="{Binding Item3}"/>
        </GridView>
      </ListView.View>
    </ListView>
    <ListView Margin="10,0,0,0" Grid.Column="1" x:Name="lv2" Visibility="Collapsed">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Item1}"/>
          <GridViewColumn Header="Größe" DisplayMemberBinding="{Binding Item2}"/>
          <GridViewColumn Header="Typ" DisplayMemberBinding="{Binding Item3}"/>
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</UserControl>
