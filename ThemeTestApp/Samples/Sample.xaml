<UserControl
  x:Class="ThemeTestApp.Samples.Sample"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:ThemeTestApp="clr-namespace:ThemeTestApp"
  mc:Ignorable="d" 
  d:DesignHeight="300"
  d:DesignWidth="300">
  <UserControl.Resources>
    <DataTemplate DataType="{x:Type ThemeTestApp:Option}">
      <CheckBox IsChecked="{Binding Enabled}" Content="{Binding Label}"/>
    </DataTemplate>
  </UserControl.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="250"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid Margin="5">
      <ListView ItemsSource="{Binding Options}"/>
    </Grid>
    <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Center" Width="3"/>
    <WindowsFormsHost Margin="0,5,5,5" Grid.Column="2" x:Name="WinFormsHost"/>
    <ContentPresenter Margin="0,5,5,5" Grid.Column="3" Content="{Binding WpfSample}"/>
  </Grid>
</UserControl>
